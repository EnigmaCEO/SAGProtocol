node_modules
server/
artifacts/
.env
.env.local
.env.*.local
frontend/.env.local
frontend/.env.*.local
# Signer service env files contain private keys — never commit
services/signer-treasury/.env
services/signer-escrow/.env
services/**/.env.local
cache/
.next/
frontend/.next/
deployments.json
.vercel

# Auto-generated by deploy.ts — not committed. Per-environment address files.
# addresses.ts is a stable committed selector; only the per-network variants are gitignored.
frontend/src/lib/addresses.local.ts
src/lib/addresses.local.ts
docs/
*.md
